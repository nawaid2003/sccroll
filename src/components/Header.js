import React from 'react';
import '../styles/Header.css';

const Header = () => (
  <div className="wrapper">
    <h1 className="title">Scroll Reveal Example</h1>
    <h3 className="subtitle">Have child elements animate after parent has revealed itself.</h3>
  </div>
);

export default Header;
